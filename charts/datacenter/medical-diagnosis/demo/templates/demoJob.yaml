apiVersion: batch/v1
kind: Job
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  name: {{ .Values.job.name }}
  namespace: {{ .Values.job.namespace }}
spec:
  template:
    spec:
      initContainers:
      - name: init-cephobjectstoreuser
        image: {{ .Values.job.image }}
        command:
        - /bin/bash
        - -c
        - |
{{- include "demo.initJob" . | nindent 10 }}
      containers: 
      - image: {{ .Values.job.image }}
        command:
        - /bin/bash
        - -c
        - |
{{- include "demo.jobs" . | nindent 10 }}
        name: {{ .Values.job.name }}
      dnsPolicy: ClusterFirst
      restartPolicy: Never
      serviceAccount: {{ .Values.job.serviceAccount }}
      serviceAccountName: {{ .Values.job.serviceAccountName }}
      terminationGracePeriodSeconds: 30

